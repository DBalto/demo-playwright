{
  "name": "newsela-assignment-qa",
  "version": "1.0.0",
  "description": "Playwright automation framework for the Newsela Writing assignment creation workflow",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.9.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.6.3"
  }
}
